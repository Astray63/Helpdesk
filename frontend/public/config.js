/**
 * Configuration runtime pour l'application Helpdesk
 * 
 * Ce fichier peut être modifié APRÈS le build pour changer l'URL de l'API
 * sans avoir à reconstruire toute l'application.
 * 
 * Sur Render, vous pouvez utiliser un script de post-build pour générer ce fichier
 * avec les bonnes valeurs.
 */
window.ENV = {
  // URL de l'API backend
  VITE_API_URL: 'https://helpdesk-backend-y73t.onrender.com'
};
